name: brik-pipe-actions — CI & Governance

on:
  pull_request:
  push:
    branches: [main]

jobs:
  governance:
    name: Governance gates
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ----------------------------------------------------------------------
      # PIPE-CORE-1.1 Governance Gates
      # ----------------------------------------------------------------------

      - name: BrikByteOS Build Contract Regression (v1)
        run: node scripts/governance/run-contract-regression.mjs
